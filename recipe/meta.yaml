{% set name = "csaps" %}
{% set version = "1.2.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/csaps-{{ version }}.tar.gz
  sha256: 5484d0f32e6186e0e4756b76f4971eb7484b41b5031fbb9b241097f3e6acb65a

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }},<4.0
    - numpy >=1.11.0,<2
    - scipy >=1.0.0,<2

test:
  imports:
    - csaps
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/espdev/csaps
  summary: Cubic spline approximation (smoothing)
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - smithsp
    - kalling
